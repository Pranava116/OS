   **Input Handling:** Created a blocking \`read_line()\` in \`input.c\` with backspace support.
   **Output:** Extended \`print.c\` to support \`\\b\` (backspace) and \`\\t\` (tab) control characters.
   **Command Dispatcher:** Added a basic \`execute_command()\` logic in \`shell.c\`.
   **Drivers:** Integrated keyboard and print drivers for interactive usage.
   EOF